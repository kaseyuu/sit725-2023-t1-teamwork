<div class="col">
    <div class="card">
        <img src="uploads/{{image}}" class="card-img-top" alt="..." />
        <div class="card-body">
            <h6 class="card-title pb-2">
                <a href="/cloth/{{id}}">{{name}}</a>
            </h6>
            <h5 class="card-title">${{price}}</h5>
        </div>
    </div>
</div>